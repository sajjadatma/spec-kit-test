import { createHash, randomUUID } from "node:crypto"; import type { PrismaClient } from "@prisma/client";
export class UploadService { constructor(private readonly prisma:PrismaClient){} async register(creatorId:string,bytes:Buffer,mediaType:string,width:number,height:number){if(bytes.length>10*1024*1024||width<256||height<256||width*height>25_000_000||!["image/jpeg","image/png","image/webp"].includes(mediaType))throw new Error("IMAGE_INVALID");const hash=createHash("sha256").update(bytes).digest("hex");return this.prisma.fileAsset.create({data:{creatorId,storageKey:`staged/${randomUUID()}`,contentHash:hash,mediaType,byteSize:bytes.length,width,height,status:"READY",validatedAt:new Date()}});}}
