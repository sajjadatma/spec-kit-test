import { describe,it,expect } from "vitest"; import { AttemptService } from "./attempt.service.js"; describe("attempt",()=>it("prevents a second active attempt",async()=>{const s=new AttemptService();await s.submit("u",{floorSelected:true,wallSelected:false,floorProductId:"p"});await expect(s.submit("u",{floorSelected:true,wallSelected:false,floorProductId:"p"})).rejects.toThrow("ATTEMPT_ACTIVE")}));
