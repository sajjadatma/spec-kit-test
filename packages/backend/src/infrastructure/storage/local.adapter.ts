import { mkdir, readFile, writeFile } from "node:fs/promises"; import { dirname, join, normalize } from "node:path"; export class LocalStorageAdapter{constructor(private readonly root:string){}private path(key:string){const path=normalize(join(this.root,key));if(!path.startsWith(normalize(this.root)))throw new Error("STORAGE_KEY_INVALID");return path}async put(key:string,body:Buffer){const path=this.path(key);await mkdir(dirname(path),{recursive:true});await writeFile(path,body)}async get(key:string){return readFile(this.path(key))}}
