import { expect,test } from "@playwright/test";test("authorized user sees dashboard navigation",async({page})=>{await page.goto("http://localhost:3000/login");await page.locator('input[name="email"]').fill("visualization@example.test");await page.locator('input[name="password"]').fill("Development-Only-Password-123!");await page.getByRole("button",{name:"Continue"}).click();await expect(page).toHaveURL(/dashboard/);await expect(page.getByRole("heading",{name:"Dashboard"})).toBeVisible();await expect(page.getByRole("navigation",{name:"Dashboard destinations"})).toBeVisible();await expect(page.getByRole("navigation",{name:"Dashboard destinations"}).getByRole("link",{name:"Products"})).toBeVisible()});
