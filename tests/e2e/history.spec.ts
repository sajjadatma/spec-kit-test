import { expect, test } from "@playwright/test";
test("authorized user can open visualization history", async ({ page }) => { await page.goto("http://localhost:3000/login"); await page.locator('input[name="email"]').fill("visualization@example.test"); await page.locator('input[name="password"]').fill("Development-Only-Password-123!"); await page.getByRole("button",{name:"Continue"}).click(); await expect(page).toHaveURL(/dashboard/); await page.goto("http://localhost:3000/visualizations"); await expect(page.getByRole("heading",{name:"Visualization history"})).toBeVisible(); });
