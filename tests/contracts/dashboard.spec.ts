import { describe,expect,it } from "vitest";import { DashboardService } from "../../packages/backend/src/dashboard/dashboard.service.js";const prisma={product:{count:()=>({})},visualizationSession:{count:()=>({})},generationAttempt:{count:()=>({})},$transaction:async()=>[2,3,1]} as never;describe("dashboard",()=>it("builds scoped destinations",async()=>{const data=await new DashboardService(prisma).summary({id:"user",role:"USER"});expect(data.destinations.map(x=>x.label)).not.toContain("User Management")}));
