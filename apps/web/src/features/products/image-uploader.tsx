"use client"; import { useState } from "react"; import { apiFetch } from "../../lib/api/client"; export function ImageUploader({productId}:{productId:string}){const [message,setMessage]=useState("");return <section><h2>Product images</h2><input aria-label="Product image" type="file" accept="image/jpeg,image/png,image/webp" onChange={async e=>{const file=e.target.files?.[0];if(!file)return;if(file.size>10*1024*1024){setMessage("Image must be 10 MiB or smaller.");return}const buffer=await file.arrayBuffer();const asset=await apiFetch<{id:string}>("/uploads",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({contentBase64:btoa(String.fromCharCode(...new Uint8Array(buffer))),mediaType:file.type,width:256,height:256})});await apiFetch(`/products/${productId}/images/${asset.id}`,{method:"POST"});setMessage("Image uploaded.");}}/>{message&&<p role="status">{message}</p>}</section>}
