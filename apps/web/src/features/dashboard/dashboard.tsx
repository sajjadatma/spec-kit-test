"use client";
import { useEffect,useState } from "react";import { apiFetch } from "../../lib/api/client";
type Data={counts:{products:number;sessions:number;active:number};destinations:{label:string;href:string}[]};export function Dashboard(){const[data,setData]=useState<Data>();useEffect(()=>{void apiFetch<Data>("/dashboard").then(setData)},[]);if(!data)return <main><h1>Dashboard</h1><p role="status">Loading dashboard…</p></main>;return <section><h1>Dashboard</h1><dl><div><dt>Products</dt><dd>{data.counts.products}</dd></div><div><dt>History sessions</dt><dd>{data.counts.sessions}</dd></div><div><dt>Active generations</dt><dd>{data.counts.active}</dd></div></dl><nav aria-label="Dashboard destinations"><ul>{data.destinations.map(destination=><li key={destination.href}><a href={destination.href}>{destination.label}</a></li>)}</ul></nav></section>}
